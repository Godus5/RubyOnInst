<div id="<%= dom_id post %>">
  <p>
    <strong>Text:</strong>
    <%= post.text %>
  </p>

  <p>
    <strong>Photo:</strong>
    <% if post.photo %>
      <%= image_tag post.photo_url(:thumb) %>
    <% else %>
      No photo available
    <% end %>
  </p>
</div>
